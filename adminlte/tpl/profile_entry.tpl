<div class="col m-0">
  <div class="info-box">
      <a href="profiles/{{$id}}">
        <img src="{{$photo}}" class="img-size-64" id="profile-listing-photo-{{$id}}" alt="{{$alt}}">
      </a>

    <div class="info-box-content">
      <span class="info-box-text"><a href="profiles/{{$id}}" class="fw-bold">{{$profile_name}}</a></span>
      <span class="info-box-number">
        {{$visible}} <!-- Name -->
      </span>
    </div>
    <!-- /.info-box-content -->
  </div>
  <!-- /.info-box -->
</div>
